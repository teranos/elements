/**
 * Re-export from @qntx/glyphs package.
 * Morphology helpers now live in packages/glyphs/.
 */
export {
    verifyGlyphAxiom,
    prepareMorphTo,
    calculateTrayTarget,
    resetGlyphElement,
} from '@qntx/glyphs';
